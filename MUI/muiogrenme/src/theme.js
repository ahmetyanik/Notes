import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
        main: "#00bc4",
        ligth: "skyblue",
    },
    secondary: {
      main: "#ff4081",
    },

    otherColor: {
      main: "#ff0000",
     
    },
  },
});
